# Cloudflare Workers Assets가 이 파일을 읽어 정적 파일에 캐시 헤더를 붙여줍니다
# (README 31번 참고). Next.js가 만드는 /_next/static/* 청크는 파일 이름에 콘텐츠
# 해시가 붙어 있어 내용이 바뀌면 파일명도 바뀌므로, 1년 장기 캐시(immutable)로
# 지정해도 안전합니다.
/_next/static/*
  Cache-Control: public,max-age=31536000,immutable
